@model MonitoriOn.Models.ViewModels.UserVM

@{
    string? className;
    if (TempData["ChangePhoneNumber"]?.ToString() == "true")
    {
        className = "open";
    }
    else
    {
        className = null;
    }
}

<div class="modal @className" onclick="closeModalDark()">
    <div class="modal-dialog">
        <div class="modal-content modal-content-expand">
            <div class="modal-content-header">
                <h2>Изменение номера телефона</h2>
                <button class="btn-modal-close" onclick="closeModalBtn()">
                    <img src="~/img/profile/modal/close.svg" />
                </button>
            </div>
            <div class="modal-content-body">
                <form asp-page-handler="ChangePhoneNumber">
                    <div class="textBlock">
                        <label asp-for="PhoneNumber"></label>
                        <input asp-for="PhoneNumber" autocomplete="off" placeholder="Введите имя" />
                        <span asp-validation-for="PhoneNumber"></span>
                    </div>
                    <div class="btnBlock">
                        <button class="btn-success">Сохранить</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>